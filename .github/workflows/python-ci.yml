name: Python CI

on: push

jobs:
  test:

    runs-on: ubuntu-18.04
    strategy:
      matrix:
        python-version:
          - "3.8"
#          - "3.9"
#          - "3.10"
#          - "3.11"

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install system-level dependencies
      run: |
        # Add postgres package repo; see https://wiki.postgresql.org/wiki/Apt
        sudo apt-get install curl ca-certificates gnupg
        curl https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
        sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
        sudo apt-get update
        sudo apt-get install postgresql-plpython3-14 postgresql-14-postgis-3

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -

    - name: Install dependencies
      run: |
        poetry env use system  # Necessary?
        poetry install

    - name: Test with pytest (full)
      if: github.ref == 'refs/heads/master'
      env:
        PYCDS_SCHEMA_NAME: other
      run: |
        poetry run pytest -v --tb=short tests

#    - name: Test with pytest (fast)
#      if: github.ref != 'refs/heads/master'
#      env:
#        PYCDS_SCHEMA_NAME: other
#      run: |
#        poetry run pytest -m "not slow" -v --tb=short tests

# FIXME: Recently got this error in CI:
#
# Run pipenv run black . --check
#Traceback (most recent call last):
#  File "/home/runner/.local/share/virtualenvs/pycds-ihtmok65/bin/black", line 8, in <module>
#    sys.exit(patched_main())
#  File "/home/runner/.local/share/virtualenvs/pycds-ihtmok65/lib/python3.8/site-packages/black.py", line 4134, in patched_main
#    patch_click()
#  File "/home/runner/.local/share/virtualenvs/pycds-ihtmok65/lib/python3.8/site-packages/black.py", line 4123, in patch_click
#    from click import _unicodefun  # type: ignore
#ImportError: cannot import name '_unicodefun' from 'click' (/home/runner/.local/share/virtualenvs/pycds-ihtmok65/lib/python3.8/site-packages/click/__init__.py)
#Error: Process completed with exit code 1.

    - name: Code format check
      if: ${{ matrix.python-version == '3.8' }}
      run: poetry run black . --check
